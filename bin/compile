#!/usr/bin/env bash
# bin/compile <build-dir> <cache_dir<

BUILD_DIR=$1
CACHE_DIR=$2

echo "SRID Compile Script called"
echo "BUILD_DIR:" $BUILD_DIR
echo "CACHE_DIR:" $CACHE_DIR

ls $BUILD_DIR/content
cat $BUILD_DIR/content/test.txt